\* SAFETY non-mix: lazy-loading evicts the VISIBLE site (drops its "never evict
\* current" guarantee). Expect: Inv_CurrentLoaded is VIOLATED with a short
\* counterexample. Demonstrates the gate catching a safety, not liveness, break.
CONSTANTS
    N = 3
    Conflict = "evict"
SPECIFICATION Spec
INVARIANTS
    TypeOK
    Inv_CurrentLoaded
PROPERTIES
    RepaintLiveness
