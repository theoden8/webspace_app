\* SAFETY non-mix: the legacy cookie engine activates a site without running
\* capture-nuke-restore, leaving another site's cookies in the shared jar.
\* Expect: Inv_JarMatchesVisible is VIOLATED (cross-site cookie leak).
CONSTANTS
    N = 3
    Conflict = "contaminate"
SPECIFICATION Spec
INVARIANTS
    TypeOK
    Inv_JarMatchesVisible
