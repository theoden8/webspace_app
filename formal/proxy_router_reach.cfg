\* Anti-vacuity: under the router two sites with DIFFERENT proxies co-load,
\* which is the cold start PROXY-008 charged. Expect:
\* Reach_MismatchedCoLoaded VIOLATED.
CONSTANTS
    N = 3
    Proxies = {"direct", "socks"}
    proxyOf <- ProxyOfDef
    Router = "on"
    Conflict = "none"
SPECIFICATION Spec
INVARIANTS
    Reach_MismatchedCoLoaded
