\* Anti-vacuity: the nuked (jar-empty) state is reachable. Expect:
\* Reach_Nuked VIOLATED (the repopulation guarantee is not vacuous).
CONSTANTS
    Conflict = "none"
SPECIFICATION Spec
INVARIANTS
    Reach_Nuked
