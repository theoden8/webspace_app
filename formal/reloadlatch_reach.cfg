\* Anti-vacuity witness: the spent-latch ordering -- every issued document has
\* settled and the surface is still owed with no nudge scheduled -- is actually
\* reachable, so the liveness check is exercised against the real failure.
\* Expect: Reach_SpentLatch VIOLATED.
CONSTANTS
    K = 2
    MaxIssue = 2
    Fix = "oneshot"
SPECIFICATION Spec
INVARIANTS
    Reach_SpentLatch
CHECK_DEADLOCK FALSE
