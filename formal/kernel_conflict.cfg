\* Non-mix demonstrator: adds a back path that bypasses the repaint chokepoint.
\* Expect: TypeOK + Inv_CurrentLoaded still hold (safety preserved), but
\* RepaintLiveness is VIOLATED with a lasso counterexample (surface wedged blank).
CONSTANTS
    N = 3
    IncludeConflict = TRUE
SPECIFICATION Spec
INVARIANTS
    TypeOK
    Inv_CurrentLoaded
PROPERTIES
    RepaintLiveness
