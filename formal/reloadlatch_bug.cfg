\* BUG-001 rapid-refresh REPRODUCTION (pre-Attempt-11): the first settle after
\* an issue consumes the one-shot latch, so a second refresh's commit -- the
\* document actually on screen -- is left blank. Expect: RepaintLiveness
\* VIOLATED.
CONSTANTS
    K = 2
    MaxIssue = 2
    Fix = "oneshot"
SPECIFICATION Spec
INVARIANTS
    TypeOK
PROPERTIES
    RepaintLiveness
CHECK_DEADLOCK FALSE
