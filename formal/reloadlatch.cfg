\* BUG-001 rapid-refresh FIXED (Attempt 11 / PAUSE-027): arming opens a window
\* the host closes on a timer, so every commit settling inside it repaints.
\* Expect: RepaintLiveness holds.
CONSTANTS
    K = 2
    MaxIssue = 2
    Fix = "window"
SPECIFICATION Spec
INVARIANTS
    TypeOK
PROPERTIES
    RepaintLiveness
CHECK_DEADLOCK FALSE
