\* Non-mix: building the drawer while the session is still locked.
\* Expect: Inv_LockedIsSealed VIOLATED.
CONSTANTS
    N = 2
    kioskOf <- KioskOfDef
    Conflict = "chrome"
SPECIFICATION Spec
INVARIANTS
    Inv_LockedIsSealed
