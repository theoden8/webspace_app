\* Proxy fail-closed, good: a proxied site never egresses directly (blanks
\* the load when the proxy can't be built). Expect: all invariants hold.
CONSTANTS
    Conflict = "none"
SPECIFICATION Spec
INVARIANTS
    TypeOK
    Inv_NoDirectWhenProxied
