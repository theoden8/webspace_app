\* LIVENESS non-mix: a back path that bypasses the repaint chokepoint (BUG-001).
\* Expect: TypeOK + Inv_CurrentLoaded still hold (safety preserved), but
\* RepaintLiveness is VIOLATED with a lasso (surface wedged blank).
CONSTANTS
    N = 3
    Conflict = "bypass"
SPECIFICATION Spec
INVARIANTS
    TypeOK
    Inv_CurrentLoaded
PROPERTIES
    RepaintLiveness
