\* BUG-001 gap #5, as a checkable difference. Attempt 8 keys the warm-start
\* repaint on didChangeMetrics: a signal that correlates with the SurfaceView
\* reattach but is not it. Model the proxy as "may or may not fire" and the
\* liveness the code claims is lost on the branch where it misses. A native
\* attach callback (Fix="attach") is exactly what removes that branch.
\* Expect: RepaintLiveness VIOLATED.
CONSTANTS
    K = 2
    Fix = "proxy"
SPECIFICATION Spec
INVARIANTS
    TypeOK
PROPERTIES
    RepaintLiveness
CHECK_DEADLOCK FALSE
