\* Non-mix: a mismatched-proxy site is co-loaded without serialisation.
\* Expect: Inv_ProxyCoherent VIOLATED.
CONSTANTS
    N = 3
    Proxies = {"direct", "socks"}
    proxyOf <- ProxyOfDef
    Conflict = "mismatch"
SPECIFICATION Spec
INVARIANTS
    Inv_ProxyCoherent
