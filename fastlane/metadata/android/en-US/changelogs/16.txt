✨ New Features:
• Full screen mode with per-site auto-fullscreen
• Import local HTML files as sites
• Per-site cookie blocking by name and domain
• DNS blocking live banner and query log
• JS console in Developer Tools

🔧 Improvements:
• Native sub-resource interceptor for DNS, LocalCDN, and ABP
• Two-step back-to-exit on Android

🐛 Bug Fixes:
• Fix user scripts not running after settings save
• Fix cross-domain navigation opening in wrong webview
