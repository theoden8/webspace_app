gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java

# Remember to never publicly share your keystore.
# See https://flutter.dev/docs/deployment/android#reference-the-keystore-from-the-app
key.properties
**/*.keystore
**/*.jks
# Gradle build outputs (reports, intermediates, classes). The path is
# relative to this file so it covers `android/build/` and `android/app/build/`.
build/
# Kotlin 2.x compile cache.
.kotlin/
